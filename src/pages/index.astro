---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";

const posts = (await getCollection("posts"))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 4);

const formatListDate = (date: Date) =>
  `${date.getUTCFullYear()}/${String(date.getUTCMonth() + 1).padStart(2, "0")}`;
---

<BaseLayout
  title="Abdul Sohu"
  description="Software engineer writing about systems, performance, and technical craft."
>
  <main class="page-shell">
    <div class="home-shell">
      <section>
        <h1>A. Sohu</h1>
        <p class="lede">
          Currently at UPenn, MSE in CIS. In my free time, I write with 
          fountain pens, and scissor switches on an M4 Max. Also, I really 
          liked Reiner Pope's personal site so I copied it (with some alterations).
        </p>
      </section>

      <section>
        <h2>Writing</h2>
        <ul class="writing-list">
          {posts.map((post) => (
            <li>
              <a class="writing-entry" href={`/posts/${post.id}/`}>
                <span class="writing-date">{formatListDate(post.data.pubDate)}</span>
                <span>{post.data.title}</span>
              </a>
            </li>
          ))}
        </ul>
      </section>

    </div>
  </main>
</BaseLayout>
